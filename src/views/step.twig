<div class="row" style="margin-bottom: 20px">
    <div class="col-md-1">
    </div>
    <div class="col-md-8">
        <i>{{ step.type }}</i>
        {{ step.text }}
    </div>
</div>

<div class="row" style="margin-bottom: 5px">
    {% for argument in step.arguments %}
        {% if argument.lines %}
            {% include 'pystring.twig' with {'lines': argument.lines} %}
        {% endif %}
        {% if argument.rows %}
            {% include 'table.twig' with {'table': argument} %}
        {% endif %}
    {% endfor %}
 </div>
